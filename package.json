{
  "name": "prero",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "npm run build && vitest run && playwright test",
    "test:unit": "vitest run tests/unit",
    "test:build": "npm run build && vitest run tests/build",
    "test:e2e": "playwright test",
    "test:watch": "vitest",
    "deploy:check": "./scripts/deploy-bluehost.sh",
    "deploy:live": "./scripts/deploy-bluehost.sh --live"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.3.1",
    "tailwindcss": "^4.3.3"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "linkedom": "^0.18.13",
    "vitest": "^5.0.0"
  }
}
